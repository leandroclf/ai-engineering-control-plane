# Agent Provider Benchmarks

Benchmarks live vendor são opt-in e devem medir latency, turns, success rate,
fallback count, token counters e reported cost sem capturar prompt/source.
Nenhum número é inventado no baseline; fixtures determinísticas validam apenas
contratos de parser, timeout, output bound e fallback.
