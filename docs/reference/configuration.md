# Configuration
The Console receives `HARNESS_URL` and its service token only on the server. Never define provider, worker-manager or service credentials with a `NEXT_PUBLIC_` prefix.
`AICP_DEMO_MODE` defaults to deterministic demo data when the Console is run without a configured Harness.
