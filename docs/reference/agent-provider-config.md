# Agent Provider Configuration

`harness/config/agent-providers.json` declara id, transport, runtime, family,
auth/billing mode, execution zone, feature flag, local-only, capabilities e
max concurrency. Não pode declarar token, secret, cookie, credential content ou
path de auth. O schema é separado do model catalog.
