# Terminology
Use `run`, `stage`, `gate`, `finding`, `evidence`, `budget`, `context provenance`, `human review`, `certification control`, `worker` and `provider alias` consistently across UI and docs.
Avoid calling an agent result an approval; only the Harness and final human/CI authority can decide lifecycle outcomes.
