import { Card, EvidenceLink, StatusBadge, Table } from "@aicp/ui";
import { demoCertification } from "@aicp/test-fixtures";

export default function ReleasePage() {
  return <div className="content"><div className="page-heading"><div><div className="eyebrow">Security & Release / Certification</div><h1>AICP v1 certification</h1><p className="muted">Directly derived from the machine-readable release contract. Current blockers stay visible.</p></div><StatusBadge status="blocked">Not yet v1 certified</StatusBadge></div><Card><div className="grid three-col"><div><div className="eyebrow">PASS</div><div className="metric-value">35</div></div><div><div className="eyebrow">BLOCKED</div><div className="metric-value">3</div></div><div><div className="eyebrow">FAILED</div><div className="metric-value">0</div></div></div></Card><Card title="Controls"><Table caption="Release controls"><thead><tr><th>Control</th><th>Status</th><th>Reason</th><th>Evidence</th></tr></thead><tbody>{demoCertification.blockers.map((item) => <tr key={item.id}><td><strong>{item.title}</strong><br /><code>{item.id}</code></td><td><StatusBadge status="blocked" /></td><td>Evidence not available in the current contract.</td><td><EvidenceLink href="/docs/release-certification">Open evidence</EvidenceLink></td></tr>)}</tbody></Table><p className="muted">PASS controls are available through the Harness API; this view does not duplicate authority or infer missing metrics.</p></Card></div>;
}
