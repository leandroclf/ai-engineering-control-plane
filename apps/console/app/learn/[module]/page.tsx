import Link from "next/link";
import { Card, CodeBlock } from "@aicp/ui";
export default async function LearnModule({ params }: { params: Promise<{ module: string }> }) { const { module } = await params; return <div className="content"><div className="page-heading"><div><div className="eyebrow">AICP Academy / {module}</div><h1>{module.replaceAll("-", " ")}</h1><p className="muted">Checkpoint: explique quem possui a autoridade e onde a evidência é persistida.</p></div><Link className="button button-secondary" href="/learn">All modules</Link></div><div className="grid two-col"><Card title="Concept"><p>O AICP separa inteligência de autoridade. OpenCode e LLMs executam trabalho não confiável; Harness controla workflow, budget, autorização, gates e terminação.</p><CodeBlock>{"Browser → BFF → Harness → Execution Plane\\nPostgreSQL = canonical state\\nNeo4j = rebuildable projection\\nRedis = ephemeral cache"}</CodeBlock></Card><Card title="Checkpoint"><p className="muted">Você consegue localizar a evidência do run em até três interações e identificar por que um controle está BLOCKED?</p><button className="button button-primary" type="button">Mark module understood</button></Card></div></div>; }
