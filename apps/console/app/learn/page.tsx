import Link from "next/link";
import { Card } from "@aicp/ui";
import { academyModules } from "@aicp/tutorial-engine";
export default function LearnPage() { return <div className="content"><div className="page-heading"><div><div className="eyebrow">AICP Academy</div><h1>Learn the control plane</h1><p className="muted">A progressive path from the first governed run to extending a worker, gate or retriever.</p></div><Link className="button button-primary" href="/learn/foundations">Start quick tour</Link></div><div className="grid two-col">{academyModules.map((module, index) => <Card key={module.id} title={`${index + 1}. ${module.title}`}><p className="muted">{module.description}</p><p className="muted">{module.estimatedMinutes} minutes · verifiable checkpoint</p><Link className="evidence-link" href={`/learn/${module.id}`}>Open module →</Link></Card>)}</div></div>; }
